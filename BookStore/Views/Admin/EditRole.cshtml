@model BookStore.ViewModels.EditRoleViewModel
@{
    ViewData["Title"] = "Edit role";
}

<style>
    .form-container {
        margin: 20px;
    }

    .title-container {
        text-align: center;
        margin-bottom: 20px;
    }

        .title-container h1 {
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5em;
            color: #343a40;
        }

            .title-container h1 i {
                margin-right: 10px;
                font-size: 1.8em;
            }

    .form-section, .user-section {
        background-color: #c3e6cb;
        border: 2px solid black;
        border-radius: 0.25rem;
        padding: 20px;
        margin-bottom: 20px;
    }

    .form-section {
        margin-top: 20px;
    }

    .user-section {
        text-align: center;
    }

    .form-group {
        margin-bottom: 15px;
    }

        .form-group label {
            font-weight: bold;
        }

        .form-group input {
            font-size: 0.9em;
        }

    .btn {
        margin-right: 10px;
    }

    .btn-warning {
        background-color: #ffc107;
        border-color: #ffc107;
    }

        .btn-warning:hover {
            background-color: #e0a800;
            border-color: #d39e00;
        }

    .btn-success {
        background-color: #28a745;
        border-color: #28a745;
    }

        .btn-success:hover {
            background-color: #218838;
            border-color: #1e7e34;
        }

    .btn-primary {
        background-color: #34a853;
        border-color: #34a853;
        margin-top: 30px;
    }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #00409b;
        }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: #f2f2f2;
    }

    .table th, .table td {
        text-align: center;
        padding: 10px;
    }

    .table {
        margin: 0 auto; /* Center the table */
        width: 80%; /* Adjust width as needed */
    }

    .float-right {
        float: right;
    }

    .user-section h2 {
        margin-bottom: 20px;
        font-size: 1.2em;
    }
</style>

<div class="form-container">
    <div class="title-container">
        <h1><i class="fa-solid fa-user-gear"></i>Edit Role</h1>
    </div>

    <form method="post" class="row">
        <div class="col-md-4 form-section">
            <h2>Role Information</h2>
            <div class="form-group row">
                <label asp-for="Id" class="col-form-label col-sm-4">Role ID:</label>
                <input asp-for="Id" disabled class="form-control col-sm-8 p-1 text-center" />
            </div>
            <div class="form-group row">
                <label asp-for="RoleName" class="col-form-label col-sm-4">Role Name:</label>
                <input asp-for="RoleName" class="form-control col-sm-8 p-2" />
                <span asp-validation-for="RoleName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <a asp-action="ListAllRoles" class="btn btn-warning"><i class="fa-solid fa-angles-left"></i> Back</a>
                <button type="submit" class="btn btn-success float-right">Update <i class="fa-solid fa-check-double"></i></button>
            </div>
        </div>

        <div class="col-md-7 user-section form-section">
            <h2>Users with @Model.RoleName role</h2>
            <table class="table table-striped">
                @if (Model.Users.Any())
                {
                    <thead>
                        <tr>
                            <th>User</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model.Users)
                        {
                            <tr>
                                <td>@user</td>
                            </tr>
                        }
                    </tbody>
                }
                else
                {
                    <tbody>
                        <tr>
                            <td>No users with @Model.RoleName role</td>
                        </tr>
                    </tbody>
                }
            </table>
            <div>
                <a asp-action="EditUsersInRole" asp-route-id="@Model.Id" class="btn btn-primary">Add/Remove users in role</a>
            </div>
        </div>
    </form>
</div>

@* 
@model BookStore.ViewModels.EditRoleViewModel
@{
    ViewData["Title"] = "Edit role";
}
<h1>Edit role</h1>

<form method="post" class="row">
    <div class="mt-3 col-4">
        <div class="form-group row">
            <label asp-for="Id" class="col-form-label col-2"></label>
            <input asp-for="Id" disabled class="form-control col-9 p-1 text-center" style="font-size: 0.9em" />
        </div>
        <div class="form-group row">
            <label asp-for="RoleName" class="col-form-label col-2"></label>
            <input asp-for="RoleName" class="form-control col-9 p-2" />
            <span asp-validation-for="RoleName" class="text-danger"></span>
        </div>

        <div asp-validation-summary="All" class="text-danger"></div>

        <div class="form-group">
            <a asp-action="ListAllRoles" class="btn btn-warning"><i class="fa-solid fa-angles-left"></i> Back</a>
            <button type="submit" class="btn btn-success float-right mr-3">Update <i class="fa-solid fa-check-double"></i></button>
        </div>
    </div>
    <div class="col-1"></div>
    <div class="col-5">
        <h3>Users with @Model.RoleName role</h3>

        <table class="table table-striped col-8">
            <tr></tr>
            @if (Model.Users.Any())
            {
                foreach (var user in Model.Users)
                {
                    <tr>
                        <td class="form-label">@user</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td class="form-label">No users with @Model.RoleName role</td>
                </tr>
            }
        </table>
        <div>
            <a asp-action="EditUsersInRole" asp-route-id="@Model.Id" class="btn btn-primary">Add/Remove users in role</a>
        </div>
    </div>
</form> *@